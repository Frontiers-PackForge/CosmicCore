dependencies {
    compileOnly(libs.jetbrains.annotations)

    // LDLib
    modImplementation(forge.ldlib)

    // Registrate
    modImplementation(forge.registrate)

    // Configuration
    modImplementation(forge.configuration)

    // GTCEu
    modImplementation(forge.gtceu)

    // Mixin (& Extras)
    annotationProcessor(variantOf(libs.mixin) { classifier("processor") })
    annotationProcessor(forge.mixinExtras.common)
    api(forge.mixinExtras.common)

    // KubeJS
    modImplementation(forge.kubejs)
    modImplementation(forge.rhino)
    modImplementation(forge.architectury)

    // Recipe Viewers
    modImplementation(forge.bundles.jei)
    modImplementation(forge.emi)

    // WAILA-likes
    modImplementation(forge.jade)

    // Curios
    modImplementation(forge.curios)

    // AE2
    modImplementation(forge.ae2)
    modImplementation(forge.modernAe2Additions)

    // LSO
    modImplementation(forge.legendarySurvivalOverhaul)

    // Ad Astra
    modRuntimeOnly(forge.yabn)
    modRuntimeOnly(forge.byteCodecs)
    modImplementation(forge.resourcefullLib)
    modImplementation(forge.resourcefullConfig)
    modImplementation(forge.botarium)
    modImplementation(forge.adAstra)

    // Blood Magic
    modImplementation(forge.bloodMagic)

    // Create
    modCompileOnly(forge.create)
    modCompileOnly(forge.flywheel)
    modImplementation(forge.geckoLib)

    // Thin air
    modCompileOnly(forge.newThinAir)

    // Puzzles Lib
    modImplementation(forge.puzzlesLib)

    // Apotheosis
    modImplementation(forge.placebo)
    modImplementation(forge.apotheosis)
    modImplementation(forge.apotheoticAttributes)

    // Patchouli
    modRuntimeOnly(forge.patchouli)

    // Embedium / Occulus
    modImplementation(forge.embeddium)
    modImplementation(forge.oculus)
    modRuntimeOnly(forge.jcpp)

    // Teams
    modImplementation(forge.ftblibrary.cm)
    modImplementation(forge.ftbteams.cm)

    //Occultism & Dependencies
    modImplementation(forge.occultism)
    modImplementation(forge.geckoLib)
    modImplementation(forge.modonomicon)
    modImplementation(forge.smartBrainLib)
    // Runtime Recipe Viewers - uncomment whichever one you want to use //
    modRuntimeOnly(forge.emi)
    // modRuntimeOnly(forge.jei.forge.impl)

    //Modern Fix
    modImplementation(forge.modernFix)
}