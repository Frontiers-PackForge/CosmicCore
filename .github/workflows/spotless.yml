# Runs formatting requirements
name: Java Formatting

on:
  push:
    branches: ['1.21.1-Neoforged']
    paths: ['src/main/java/**']
  pull_request:
    paths: ['**']

concurrency:
  group: formatting-${{ github.ref }}
  cancel-in-progress: true

jobs:
  formatting:
    name: Formatting
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: read
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
      - name: Check Path Filter
        uses: dorny/paths-filter@v3
        id: filter
        with:
          filters: |
            code:
              - 'src/main/java/**'
      - name: Setup Build
        if: steps.filter.outputs.code == 'true'
        uses: ./.github/actions/build_setup
      - name: Run Spotless Formatting Check with Gradle
        if: steps.filter.outputs.code == 'true'
        run: ./gradlew -Dorg.gradle.java.home="$JAVA_HOME" spotlessCheck --warning-mode all --build-cache --no-daemon
